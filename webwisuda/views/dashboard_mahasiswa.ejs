<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profil Mahasiswa - Wisuda Polinela</title>
  <link rel="shortcut icon" href="/images/logopolinela.png" type="image/png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/profile.css">
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-custom">
  <div class="container">
    <a class="navbar-brand" href="/mahasiswa/dashboard">
      <i class="fas fa-graduation-cap me-2"></i>Profil Mahasiswa
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon" style="filter: brightness(0) invert(1);"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <li class="nav-item">
          <a class="nav-link active" href="/mahasiswa/dashboard">Dashboard</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/auth/logout">Logout</a>
        </li>
        <li class="nav-item ms-3">
          <div class="nav-avatar-img"> 
            <% if (mahasiswa.foto_profil) { %>
              <img src="/uploads/profile/<%= mahasiswa.foto_profil %>" alt="Foto Profil">
            <% } else { %>
              <img src="https://ui-avatars.com/api/?name=<%= mahasiswa.nama %>&size=120&background=0047AB&color=fff&bold=true" alt="Avatar">
            <% } %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="main-content">
  <div class="container">
    <h1 class="page-title">Profil Mahasiswa</h1>
    <p class="page-subtitle">Lihat dan kelola informasi pribadi serta status pendaftaran wisuda Anda.</p>
    
    <div class="row mb-4">
      <div class="col-md-12">
        <% if (typeof periode !== 'undefined' && periode) { %>
          
          <% 
            // Hitung persentase sisa kuota
            let sisa = periode.kuota - periode.terisi;
            let persenTerisi = (periode.terisi / periode.kuota) * 100;
            let warnaBar = sisa < 10 ? 'bg-danger' : 'bg-success';
          %>

          <div class="card shadow-sm border-0 mb-3" style="background: #fff; border-radius: 15px;">
            <div class="card-body p-4">
              <div class="d-flex justify-content-between align-items-center mb-3">
                <h5 class="fw-bold text-primary mb-0"><i class="fas fa-calendar-alt me-2"></i><%= periode.nama_periode %></h5>
                <span class="badge bg-secondary">Batas Akhir: <%= typeof tenggatWaktu !== 'undefined' ? tenggatWaktu : '-' %></span>
              </div>
              <hr>
              
              <div class="row text-center mb-3">
                <div class="col-4 border-end">
                  <h6 class="text-muted small text-uppercase ls-1">Total Kuota</h6>
                  <h3 class="fw-bold mb-0"><%= periode.kuota %></h3>
                </div>
                <div class="col-4 border-end">
                  <h6 class="text-muted small text-uppercase ls-1">Terisi</h6>
                  <h3 class="fw-bold text-warning mb-0"><%= periode.terisi %></h3>
                </div>
                <div class="col-4">
                  <h6 class="text-muted small text-uppercase ls-1">Sisa Slot</h6>
                  <h3 class="fw-bold text-danger mb-0"><%= sisa %></h3>
                </div>
              </div>

              <div class="mt-2">
                <div class="d-flex justify-content-between mb-1">
                  <span class="small text-muted">Progress Pendaftaran</span>
                  <span class="small fw-bold"><%= Math.round(persenTerisi) %>% Terisi</span>
                </div>
                <div class="progress" style="height: 15px; border-radius: 10px;">
                  <div class="progress-bar progress-bar-striped progress-bar-animated <%= warnaBar %>" 
                       role="progressbar" 
                       style="width: <%= persenTerisi %>%;" 
                       aria-valuenow="<%= periode.terisi %>" 
                       aria-valuemin="0" 
                       aria-valuemax="<%= periode.kuota %>">
                  </div>
                </div>
              </div>

              <% if (sisa <= 0) { %>
                <div class="alert alert-danger mt-3 mb-0 d-flex align-items-center">
                  <i class="fas fa-exclamation-triangle me-3 fs-4"></i>
                  <div>
                    <strong>Mohon Maaf!</strong> Kuota pendaftaran untuk periode ini sudah <strong>PENUH</strong>.
                  </div>
                </div>
              <% } else if (pendaftaran && pendaftaran.status === 'Draft' && !isClosed) { %>
                <div class="alert alert-info mt-3 d-flex align-items-center shadow-sm">
                  <i class="fas fa-info-circle me-3 fs-4 text-primary"></i>
                  <div class="flex-grow-1">
                    <strong>Berkas Anda siap!</strong> Status saat ini masih <strong>Draft</strong>. 
                    Segera kunci slot wisuda Anda sebelum kehabisan.
                  </div>
                  <form action="/mahasiswa/ajukan-verifikasi" method="POST" class="ms-3">
                    <button type="submit" class="btn btn-success fw-bold px-4 py-2" onclick="return confirm('Apakah Anda yakin data sudah benar? Data tidak bisa diubah setelah diajukan.')">
                      <i class="fas fa-check-circle me-2"></i>Ambil Slot & Finalisasi
                    </button>
                  </form>
                </div>
              <% } %>

            </div>
          </div>

        <% } else { %>
          <div class="alert alert-warning d-flex align-items-center">
            <i class="fas fa-clock me-3 fs-4"></i>
            <div>
              <strong>Belum Dibuka.</strong> Saat ini tidak ada periode wisuda yang aktif. Silakan tunggu informasi selanjutnya.
            </div>
          </div>
        <% } %>
      </div>
    </div>
    <div class="row">
      <div class="col-lg-8">
        <div class="profile-card">
          <div class="profile-header">
            <div class="profile-avatar">
              <% if (mahasiswa.foto_profil) { %>
                <img src="/uploads/profile/<%= mahasiswa.foto_profil %>" alt="Foto Profil">
              <% } else { %>
                <img src="https://ui-avatars.com/api/?name=<%= mahasiswa.nama %>&size=120&background=0047AB&color=fff&bold=true" alt="Avatar">
              <% } %>
            </div>
            <div class="profile-info flex-grow-1">
              <h2><%= mahasiswa.nama %></h2>
              <p class="role">Mahasiswa</p>
              <p class="faculty">Jurusan <%= mahasiswa.jurusan %></p>
            </div>
            <button class="btn-edit-profile" onclick="window.location.href='/mahasiswa/edit-profile'">
              <i class="fas fa-pen me-2"></i>Edit Profil
            </button>
          </div>
          
          <div class="profile-details">
            <div class="detail-row">
              <div class="detail-label">NIM</div>
              <div class="detail-value"><%= mahasiswa.nim %></div>
            </div>
            <div class="detail-row">
              <div class="detail-label">Program Studi</div>
              <div class="detail-value"><%= mahasiswa.prodi %></div>
            </div>
            <div class="detail-row">
              <div class="detail-label">Email</div>
              <div class="detail-value"><%= user.email %></div>
            </div>
            <div class="detail-row">
              <div class="detail-label">No. HP</div>
              <div class="detail-value"><%= mahasiswa.no_hp || '-' %></div>
            </div>
            <div class="detail-row">
              <div class="detail-label">Angkatan</div>
              <div class="detail-value"><%= mahasiswa.angkatan || '-' %></div>
            </div>
            <div class="detail-row">
              <div class="detail-label">IPK</div>
              <div class="detail-value"><%= mahasiswa.ipk || '-' %></div>
            </div>
            <div class="detail-row">
              <div class="detail-label">Tahun Lulus</div>
              <div class="detail-value"><%= mahasiswa.tahun_lulus || '-' %></div>
            </div>
          </div>
        </div>
        
        <div class="quick-access">
          <h3>Akses Cepat</h3>
          <div class="access-grid">
            <a href="/mahasiswa/riwayat" class="access-card">
              <i class="fas fa-history"></i>
              <h4>Riwayat Pendaftaran</h4>
              <p>Lihat riwayat pendaftaran wisuda Anda sebelumnya.</p>
            </a>
            
            <% if (typeof isClosed !== 'undefined' && isClosed === true) { %>
              <div class="access-card" style="background-color: #f8f9fa; cursor: not-allowed; border: 1px solid #ddd;">
                <i class="fas fa-lock text-secondary"></i>
                <h4 class="text-muted">Upload Berkas</h4>
                <p class="text-danger fw-bold" style="font-size: 0.85rem;">
                  <i class="fas fa-times-circle me-1"></i>Pendaftaran Ditutup
                </p>
              </div>
            <% } else { %>
              <a href="/mahasiswa/upload-berkas" class="access-card">
                <i class="fas fa-file-upload"></i>
                <h4>Upload Berkas</h4>
                <p>Unggah atau perbaharui berkas pendaftaran wisuda Anda.</p>
              </a>
            <% } %>

          </div>
        </div>
      </div>
      
      <div class="col-lg-4">
        <div class="status-card">
          <h3>Status Pendaftaran Wisuda</h3>
          
          <% if (pendaftaran) { %>
            <% if (pendaftaran.status === 'Diverifikasi') { %>
              <div class="status-badge">
                <i class="fas fa-check-circle"></i>
                <strong>Terverifikasi</strong>
              </div>
              <p class="status-message">
                Selamat! Pendaftaran wisuda Anda telah diverifikasi oleh fakultas.
              </p>
              <button class="btn-primary-custom" onclick="window.location.href='/mahasiswa/detail-pendaftaran'">
                <i class="fas fa-eye me-2"></i>Lihat Detail Pendaftaran
              </button>
              <button class="btn-secondary-custom" onclick="window.location.href='/mahasiswa/cetak-bukti'">
                <i class="fas fa-download me-2"></i>Unduh Bukti Daftar
              </button>
            
            <% } else if (pendaftaran.status === 'Diajukan') { %>
              <div class="status-badge pending">
                <i class="fas fa-clock"></i>
                <strong>Menunggu Verifikasi</strong>
              </div>
              <p class="status-message">
                Berkas Anda sedang dalam proses verifikasi. Harap tunggu konfirmasi dari fakultas.
              </p>
              <button class="btn-primary-custom" onclick="window.location.href='/mahasiswa/detail-pendaftaran'">
                <i class="fas fa-eye me-2"></i>Lihat Detail Pendaftaran
              </button>

            <% } else if (pendaftaran.status === 'Draft') { %>
              <div class="status-badge bg-secondary text-white">
                <i class="fas fa-pencil-alt"></i>
                <strong>Draft (Belum Final)</strong>
              </div>
              <p class="status-message">
                Anda sudah mengupload berkas tapi belum melakukan finalisasi. Slot belum aman!
              </p>
              <button class="btn-primary-custom" onclick="window.location.href='/mahasiswa/upload-berkas'">
                <i class="fas fa-edit me-2"></i>Lengkapi Berkas
              </button>

            <% } else if (pendaftaran.status === 'Ditolak') { %>
              <div class="status-badge rejected">
                <i class="fas fa-times-circle"></i>
                <strong>Ditolak</strong>
              </div>
              <p class="status-message">
                <strong>Catatan dari Admin:</strong><br>
                <%= pendaftaran.catatan || 'Mohon perbaiki dokumen yang ditolak.' %>
              </p>
              
              <% if (typeof isClosed !== 'undefined' && isClosed === true) { %>
                  <div class="alert alert-danger mt-3 py-2" style="font-size: 0.9rem;">
                      <i class="fas fa-lock me-1"></i> <strong>Waktu Habis.</strong> Tidak dapat perbaiki berkas.
                  </div>
              <% } else { %>
                  <button class="btn-primary-custom" onclick="window.location.href='/mahasiswa/upload-berkas'">
                    <i class="fas fa-redo me-2"></i>Unggah Ulang Berkas
                  </button>
              <% } %>

            <% } %>
          <% } else { %>
            <% if (typeof isClosed !== 'undefined' && isClosed === true) { %>
              <div class="alert alert-danger" style="background-color: #ffebee; color: #c62828; border: 1px solid #ffcdd2;">
                <div class="d-flex align-items-start">
                  <i class="fas fa-lock mt-1 me-2"></i>
                  <div>
                    <strong>Pendaftaran Ditutup</strong>
                    <p class="mb-0 small mt-1">
                      Mohon maaf, batas waktu pendaftaran wisuda telah berakhir pada:
                    </p>
                    <div class="badge bg-danger mt-2 text-wrap" style="font-size: 0.85rem; padding: 6px 10px;">
                      <i class="far fa-calendar-alt me-1"></i>
                      <%= typeof tenggatWaktu !== 'undefined' ? tenggatWaktu : '-' %>
                    </div>
                  </div>
                </div>
              </div>
              <button class="btn w-100" disabled style="background-color: #e9ecef; color: #6c757d; cursor: not-allowed; padding: 12px; border-radius: 10px; border: 1px solid #dee2e6; font-weight: 600;">
                <i class="fas fa-ban me-2"></i>Akses Ditutup
              </button>

            <% } else { %>
              <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i>
                Anda belum melakukan pendaftaran wisuda.
                <hr style="margin: 8px 0; border-top: 1px dashed #b6effb;">
                <small class="d-block fw-bold">
                  <i class="fas fa-calendar-alt me-1"></i> 
                  Batas Akhir: <%= typeof tenggatWaktu !== 'undefined' ? tenggatWaktu : '-' %>
                </small>
              </div>
              <button class="btn-primary-custom" onclick="window.location.href='/mahasiswa/upload-berkas'">
                <i class="fas fa-plus me-2"></i>Daftar Wisuda Sekarang
              </button>
            <% } %>

          <% } %>
        </div>
      </div>
    </div>
  </div>
</div>

<footer class="footer">
  <p>&copy; 2025 Politeknik Negeri Lampung. All rights reserved.</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>
  <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
    Swal.fire({
      icon: 'success',
      title: 'Berhasil!',
      text: '<%= success_msg %>',
      confirmButtonColor: '#0047AB'
    });
  <% } %>
  
  <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
    Swal.fire({
      icon: 'error',
      title: 'Perhatian!',
      text: '<%= error_msg %>',
      confirmButtonColor: '#d33'
    });
  <% } %>
</script>

</body>
</html>
