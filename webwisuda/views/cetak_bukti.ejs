<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Bukti Pendaftaran Wisuda - <%= data.nim %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { font-family: 'Times New Roman', Times, serif; color: #000; background: #fff; }
    .page-container { width: 210mm; min-height: 297mm; margin: 0 auto; padding: 20mm; position: relative; }
    
    /* Kop Surat */
    .kop-surat { border-bottom: 3px double #000; padding-bottom: 10px; margin-bottom: 30px; display: flex; align-items: center; justify-content: center; text-align: center; }
    .logo { width: 80px; height: auto; position: absolute; left: 20mm; top: 20mm; }
    .kop-text h3 { margin: 0; font-weight: bold; font-size: 18px; text-transform: uppercase; }
    .kop-text h4 { margin: 0; font-weight: bold; font-size: 16px; }
    .kop-text p { margin: 0; font-size: 12px; }

    .judul-surat { text-align: center; margin-bottom: 30px; }
    .judul-surat h4 { text-decoration: underline; font-weight: bold; margin-bottom: 5px; }
    
    .table-data tr td { padding: 5px; vertical-align: top; }
    .label-col { width: 180px; font-weight: bold; }
    
    .box-status { border: 2px solid #000; padding: 10px; text-align: center; margin: 20px 0; font-weight: bold; width: fit-content; }
    
    .ttd-area { margin-top: 50px; float: right; text-align: center; width: 200px; }

    /* Tombol Print (Hilang saat diprint) */
    .no-print { position: fixed; top: 20px; right: 20px; }

    @media print {
      .no-print { display: none !important; }
      body { -webkit-print-color-adjust: exact; margin: 0; }
      .page-container { width: 100%; margin: 0; padding: 0; border: none; }
    }
  </style>
</head>
<body>

  <div class="no-print">
    <button onclick="window.print()" class="btn btn-primary btn-lg">
      <i class="fas fa-download"></i> Download PDF / Cetak
    </button>
    <a href="/mahasiswa/dashboard" class="btn btn-secondary btn-lg">Kembali</a>
  </div>

  <div class="page-container">
    <div class="kop-surat">
      <img src="/images/logopolinela.png" alt="Logo Polinela" class="logo">
      <div class="kop-text" style="width: 100%; padding-left: 60px;">
        <h3>KEMENTERIAN PENDIDIKAN, KEBUDAYAAN,<br>RISET, DAN TEKNOLOGI</h3>
        <h4>POLITEKNIK NEGERI LAMPUNG</h4>
        <p>Jl. Soekarno Hatta No. 10, Rajabasa, Bandar Lampung 35144<br>
        Telp: (0721) 703995, Fax: (0721) 787309<br>
        Website: www.polinela.ac.id</p>
      </div>
    </div>

<div class="judul-surat">
      <h4>BUKTI PENDAFTARAN WISUDA</h4>
      <span>Tahun Akademik 2024/2025</span>
    </div>

    <p>Menerangkan bahwa mahasiswa di bawah ini telah melakukan pendaftaran wisuda secara online:</p>

    <table class="table-data" style="width: 100%;">
      <tr>
        <td class="label-col">Nama Lengkap</td>
        <td>: <%= data.nama %></td>
      </tr>
      <tr>
        <td class="label-col">NIM</td>
        <td>: <%= data.nim %></td>
      </tr>
      <tr>
        <td class="label-col">Program Studi</td>
        <td>: <%= data.prodi %></td>
      </tr>
      <tr>
        <td class="label-col">Jurusan</td>
        <td>: <%= data.jurusan %></td>
      </tr>
      <tr>
        <td class="label-col">No. Handphone</td>
        <td>: <%= data.no_hp %></td>
      </tr>
      <tr>
        <td class="label-col">Tanggal Daftar</td>
        <td>: <%= new Date(data.tanggal_daftar).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %></td>
      </tr>
    </table>
    <div class="mt-4">
      <strong>Status Verifikasi Berkas:</strong>
      <table class="table table-bordered table-sm mt-2" style="font-size: 14px; border-color: #000;">
        <thead>
          <tr>
            <th style="width: 5%;">No</th>
            <th>Jenis Dokumen</th>
            <th style="width: 20%;">Status</th>
          </tr>
        </thead>
        <tbody>
          <% dokumen.forEach((doc, index) => { %>
          <tr>
            <td class="text-center"><%= index + 1 %></td>
            <td><%= doc.jenis_dokumen.replace(/_/g, ' ') %></td>
            <td class="text-center"><%= doc.status %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <div class="box-status">
      STATUS: <%= data.status.toUpperCase() %>
    </div>

    <p style="font-size: 12px; font-style: italic;">
      *Simpan bukti pendaftaran ini sebagai syarat pengambilan atribut wisuda.
    </p>

    <div class="ttd-area">
      <p>Bandar Lampung, <%= new Date().toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %></p>
      <p>Mahasiswa Pendaftar,</p>
      <br><br><br>
      <p style="font-weight: bold; text-decoration: underline;"><%= data.nama %></p>
      <p>NIM. <%= data.nim %></p>
    </div>
  </div>

  <script>
    // Otomatis buka dialog print saat halaman dimuat
    window.onload = function() {
      window.print();
    }
  </script>
</body>
</html>